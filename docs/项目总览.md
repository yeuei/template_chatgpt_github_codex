# 项目总览

> 本文件描述当前项目“是什么、为什么做、有哪些阶段、当前主线是什么”。
> 只保留当前有效版本；历史通过 Git 查看。

## 1. 项目名称

`<填写项目名称>`

## 2. 项目目标

用 3–8 条描述项目的最终目标。例如：

- 训练一个能够正确使用工具、并减少不必要工具调用的策略模型；
- 在保持准确率的前提下降低平均工具调用次数；
- 建立可复现实验与审计链路。

## 3. 当前主线

```text
阶段A
  ↓
阶段B
  ↓
阶段C
```

## 4. 当前阶段

- 当前阶段：`<填写>`
- 当前主要仓库：`<owner/repo>`
- 当前关键 PR：`#<N>`

## 5. 当前不变量

这些是 Agent/ChatGPT 默认不得擅自改变的项目级约束。例如：

- 正式模型：`<model>`
- 工具：`<tool list>`
- 训练框架：`<framework>`
- 正式 benchmark：`<benchmark>`
- 其它：`<...>`

## 6. 当前未决项

只列真正尚未决策、会影响项目方向的问题。

- `<问题1>`
- `<问题2>`

普通工程细节不要放这里。

## 7. 阅读路径

第一次进入项目：

1. 本文件；
2. `docs/技术规范.md` 的总流程与当前任务相关章节；
3. 当前 PR 的 `coordination/PR-<N>/任务.md`；
4. 如果是 ChatGPT，再读 `agent汇报.md`；
5. 如果是 Agent，再读 `chatgpt解惑.md`。
